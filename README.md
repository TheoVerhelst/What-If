# What-If

Implementation of several estimators of counterfactual probabilities
and wrappers for uplift models (plus some additional code for benchmarks).

## Folders

 - `whatif/`: code of the package
 - `examples/`: Jupyter notebooks containing examples of how to use the library
 - `docs/`: configuration to build the package documentation with Sphinx